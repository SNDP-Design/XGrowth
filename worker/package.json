{
  "name": "xgrowth-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker proxy that calls Google Gemini for XGrowth Content Engine",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
