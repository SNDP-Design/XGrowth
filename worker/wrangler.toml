name = "xgrowth-api"
main = "src/index.js"
compatibility_date = "2024-12-01"

# Pinned so deploys skip the flaky "retrieve account IDs" auto-lookup.
# (Account ID is not a secret — the Gemini key stays a Wrangler secret.)
account_id = "8b888f92ac06f3ad4fead2a17e74233d"

# The Worker will be served at:
#   https://xgrowth-api.xgrowth.workers.dev

[ai]
binding = "AI"
